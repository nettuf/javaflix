import styled from "styled-components";

export const FooterBase = styled.footer`
    color: var(--white);
    font-size: 1.2rem;
    a:hover {
        color: var(--blackLighter);
        font-size: 1.208rem;
        transition: 0.2s;
    }
    p {
        font-size: 1.05rem;
    }
`;